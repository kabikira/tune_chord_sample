import 'package:flutter/material.dart';

class CodeFormEdit extends StatelessWidget {
  const CodeFormEdit({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('CodeFormEdit')),
      body: const Center(child: Text('CodeFormEdit')),
    );
  }
}
